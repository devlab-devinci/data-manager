{% extends 'base.html.twig' %}

{% block title %}Box{% endblock %}

{% block body %}
    <h1>Box</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ box.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ box.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ box.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_box_index') }}">back to list</a>

    <a href="{{ path('admin_box_edit', {'id': box.id}) }}">edit</a>

    {{ include('Admin/box/_delete_form.html.twig') }}
{% endblock %}
